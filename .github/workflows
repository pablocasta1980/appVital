name: CI-CD appVital

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🔹 Clonar repositorio
        uses: actions/checkout@v4

      - name: 🔹 Configurar Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🔹 Dar permisos a Gradle
        run: chmod +x gradlew

      - name: 🔹 Compilar y probar
        run: ./gradlew clean build --no-daemon

      - name: 🔹 Construir imagen Docker
        run: docker build -t appvital:latest .

      # Descomenta las siguientes líneas si quieres subir la imagen a Docker Hub
      # - name: 🔹 Iniciar sesión en Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: 🔹 Etiquetar y subir imagen
      #   run: |
      #     docker tag appvital:latest ${{ secrets.DOCKER_USERNAME }}/appvital:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/appvital:latest
